#include <stdio.h>
#include <stdlib.h>

int main (void) {

    int N;

    printf("Apresentando numeros de 20 a 35\n");
    for(N= 20; N <= 35; N++)
    {
        printf("%d\t", N);
    }

    return (0);
}